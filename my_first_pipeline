pipeline {
    agent any

    stages {
        stage('Greet') {
            steps {
                echo 'Hello, Jenkins!'
            }
        }

        stage('System Info') {
            steps {
                sh 'uname -a'        // Prints Linux system info
                sh 'whoami'          // Shows current user
            }
        }
    }
}
