pipeline {
    agent {
        docker { image 'node:18-alpine' } // Lightweight Node.js image
    }

    stages {
        stage('Greet') {
            steps {
                echo 'Hello from Docker-based Jenkins agent!'
            }
        }
    }
}
